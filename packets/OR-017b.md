# OR-017b — call panel

Branch: feat/call-panel

Call opens a panel under the entry with phone, email, address, and the
note's own record and loan blocks. "Mark as called" and "Not now" persist
to call_log (created by migration 0002) and undo for five seconds. A
dismissed name counts as shown for next month's suppression. A paused
account still gets build_call_lists; compose and send do not run.
