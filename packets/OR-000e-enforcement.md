# OR-000e — enforcement, not instruction

Branch: fix/enforcement-rules

Record the lesson from OR-000d and OR-017b in PROJECT_STATE.md, add
"DROP TABLE IF EXISTS in a migration to clean up a rogue table" to the
rejected ideas, and make AGENT_ROLES.md say Cursor is closed, not
instructed, while a packet branch is open. Documentation only.
