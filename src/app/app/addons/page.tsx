export default function AddonsPage() {
  return (
    <main className="px-4 py-10">
      <h1 className="text-[22px] font-semibold">Add-ons</h1>
      <p className="mt-3 max-w-xl text-[15px]">
        Add-ons are not available yet. This page is here so the top bar has a real
        destination.
      </p>
    </main>
  )
}
